/**
 * @author evivehealth on 10/04/18.
 */

const initialState = {
  testValue:  'test Value from redux',
};

const ReactTrainingHomeReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
};

export default ReactTrainingHomeReducer;
